apiVersion: v1
kind: Service
metadata:
  name: platform-api
  labels:
    app: platform-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: platform-api
