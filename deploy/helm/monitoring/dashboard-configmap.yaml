# ConfigMap for custom Grafana dashboards
# The kube-prometheus-stack Grafana sidecar auto-discovers ConfigMaps
# with label grafana_dashboard: "1" and loads them as dashboards
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards-custom
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
