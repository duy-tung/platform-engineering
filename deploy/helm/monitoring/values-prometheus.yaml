# kube-prometheus-stack — lean config for learning (e2-medium nodes)
prometheus:
  prometheusSpec:
    retention: 7d
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        cpu: 500m
        memory: 512Mi
    storageSpec:
      volumeClaimTemplate:
        spec:
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 10Gi
    # Tolerate app-pool nodes
    tolerations:
      - key: "spot"
        operator: "Equal"
        value: "true"
        effect: "PreferNoSchedule"

grafana:
  adminPassword: "admin"  # Change in prod
  # Serve Grafana under /grafana sub-path behind GKE L7 LB
  grafana.ini:
    server:
      domain: platform.duy-tung.dev
      root_url: "%(protocol)s://%(domain)s/grafana/"
      serve_from_sub_path: true
  # Probes must use sub-path
  readinessProbe:
    httpGet:
      path: /grafana/api/health
      port: 3000
  livenessProbe:
    httpGet:
      path: /grafana/api/health
      port: 3000
    initialDelaySeconds: 60
    timeoutSeconds: 30
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      cpu: 200m
      memory: 256Mi
  tolerations:
    - key: "spot"
      operator: "Equal"
      value: "true"
      effect: "PreferNoSchedule"

alertmanager:
  alertmanagerSpec:
    resources:
      requests:
        cpu: 10m
        memory: 32Mi
      limits:
        cpu: 100m
        memory: 64Mi
    tolerations:
      - key: "spot"
        operator: "Equal"
        value: "true"
        effect: "PreferNoSchedule"

# Node exporter on all nodes
nodeExporter:
  tolerations:
    - effect: NoSchedule
      operator: Exists

# Lightweight kube-state-metrics
kube-state-metrics:
  resources:
    requests:
      cpu: 10m
      memory: 32Mi
    limits:
      cpu: 100m
      memory: 128Mi
